week02
-linux command : ls / ls -a/ ls -al /pwd /cd /git /cd ../ echo/ cat
-git command :
git status
git add week01.txt
git config --global user.email "example@.example.com" 
git config --global user.name "example@.example.com"
git commit -m "v0"
git log 
git checkout 

git branch -M main
git remote add origin https://github.com/DRation1/test2.git
git push -u origin main
git push

git checkout specific-commit-id
git checkout . 취소
